{
  "name": "korczak-xyz-functions",
  "description": "The site's backend: the Event Watch and Metro Watch collectors, the web-push sender, and the sloper video assembler.",
  "private": true,
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google/genai": "^2.19.0",
    "busboy": "^1.6.0",
    "ffmpeg-static": "^5.2.0",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/busboy": "^1.5.4",
    "@types/node": "^22.10.5",
    "@types/web-push": "^3.6.4",
    "firebase": "^12.19.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
