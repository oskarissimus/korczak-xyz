---
import Layout from '../../../../layouts/Layout.astro';
import EventsPipeline from '../../../../components/Events/EventsPipeline';
import EventsTabs from '../../../../components/Events/EventsTabs.astro';
import EventsPipelineSkeleton from '../../../../components/Events/skeletons/EventsPipelineSkeleton.astro';
import { useTranslations } from '../../../../i18n';
import '../../../../styles/events.css';

const lang = 'pl';
const t = useTranslations(lang);
---

<Layout title={`${t('Events')} | korczak.xyz`} lang={lang} pwa="events">
  <div class="window events-window">
    <div class="title-bar">
      <span>{t('Events')}</span>
      <div class="title-buttons">
        <div class="title-btn minimize-btn"></div>
        <div class="title-btn maximize-btn"></div>
        <div class="title-btn close-btn"></div>
      </div>
    </div>
    <div class="window-content events-content">
      <EventsTabs lang={lang} active="pipeline" />
      <EventsPipeline client:only="react" lang={lang}>
        <EventsPipelineSkeleton slot="fallback" lang={lang} />
      </EventsPipeline>
    </div>
  </div>
</Layout>
